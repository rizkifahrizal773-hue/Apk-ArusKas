<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/space_xl"
    android:background="@color/colorSurface">

    <!-- Handle line indicator for BottomSheet -->
    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="@dimen/space_l"
        android:background="@color/colorBorder"
        android:backgroundTint="@color/colorBorder" />

    <TextView
        android:id="@+id/text_dialog_title"
        style="@style/TextAppearance.ArusKas.Title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/space_xl"
        android:text="Atur Anggaran Belanja"
        android:textSize="18sp" />

    <!-- 1. Category Selection Dropdown (Hidden in Edit Mode) -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/input_category_layout"
        style="@style/Widget.Material3.TextInputLayout.OutlinedBox.ExposedDropdownMenu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/space_l"
        android:hint="Pilih Kategori Pengeluaran"
        app:boxCornerRadiusTopStart="@dimen/input_corner_radius"
        app:boxCornerRadiusTopEnd="@dimen/input_corner_radius"
        app:boxCornerRadiusBottomStart="@dimen/input_corner_radius"
        app:boxCornerRadiusBottomEnd="@dimen/input_corner_radius"
        app:boxStrokeColor="@color/colorPrimary"
        app:hintTextColor="@color/colorPrimary"
        app:errorEnabled="true">

        <AutoCompleteTextView
            android:id="@+id/input_category"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="none" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Category static label (Shown in Edit Mode instead of dropdown) -->
    <TextView
        android:id="@+id/text_category_static"
        style="@style/TextAppearance.ArusKas.Title"
        android:textSize="16sp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/space_l"
        android:text="Kategori: Makanan"
        android:visibility="gone" />

    <!-- 2. Budget Limit Input -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/input_limit_layout"
        style="@style/Widget.ArusKas.TextInputLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/space_xl"
        android:hint="Batas Anggaran Bulanan"
        app:prefixText="Rp "
        app:prefixTextColor="@color/colorTextPrimary"
        app:errorEnabled="true">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/input_limit"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="number"
            android:maxLines="1"
            style="@style/TextAppearance.ArusKas.Number"
            android:textSize="16sp" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Save Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_save_budget"
        style="@style/Widget.Material3.Button"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:backgroundTint="@color/colorPrimary"
        android:textColor="@color/white"
        android:text="Simpan Anggaran"
        app:cornerRadius="@dimen/button_corner_radius" />

    <!-- Delete Button (Visible only in Edit Mode) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_delete_budget"
        style="@style/Widget.Material3.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginTop="@dimen/space_m"
        android:textColor="@color/colorExpense"
        app:strokeColor="@color/colorExpense"
        app:strokeWidth="1dp"
        android:text="Hapus Anggaran"
        app:cornerRadius="@dimen/button_corner_radius"
        android:visibility="gone" />

</LinearLayout>
